# Users module

This module owns user profile and identity records.

## Files

- `mod.rs` exports the users module files.
- `model.rs` defines Rust structs that represent user database records.
- `repository.rs` will contain SQLx queries for reading and updating users.
- `service.rs` will contain user-related business rules.
