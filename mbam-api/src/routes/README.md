# Routes

This folder contains top-level routes that do not belong to a specific domain module.

## Files

- `mod.rs` combines top-level routes into one router.
- `health.rs` exposes the health check endpoint used by local development, monitoring, and deployment checks.
